---
- hosts: mywebservers
  become: yes
  gather_facts: yes
  tasks:
  - name: i want to install java on Ubuntu
    apt:
     name: "{{myjavapackonubuntu}}"
     state: absent
    when: ansible_os_family == "Debian"
