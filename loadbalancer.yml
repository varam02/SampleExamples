---
- hosts: all
  become: yes
  
  tasks:
    - name: Update APT package cache
      apt:
      update_cache: yes
    - name: Install nginx
      apt:
       - name: nginx
         state: present
    - name: start nginx
      apt:
       - name: nginx
         state: started

        