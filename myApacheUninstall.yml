---
- hosts: 10.0.0.5
  become: yes
  tasks:
  - name: install apache2
    apt: 
     name: apache2
     state: present
    notify: stopapache2
  handlers:
    - name: stopapache2
      service:
        name: apache2
        state: stopped
